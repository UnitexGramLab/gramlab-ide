<project name="Gramlab" default="install" basedir=".">

	<description>simple example build file</description>

	<!-- set global properties for this build -->

	<property name="src" location="src" />
	<property name="build" location="build" />
	<property name="dist" location="dist" />
	<property environment="env"/>
	<input
	    message="Please enter Unitex Home (default is env var UNITEX_HOME):"
	    addproperty="unitexHome"
	    defaultvalue="${env.UNITEX_HOME}"
	    />
	<property name="appdir" location="${unitexHome}/App" />

	<!-- targets -->
	<target name="init">
		<!-- Create the time stamp -->
		<tstamp />
		<!-- Create the build directory structure used by compile -->
		<mkdir dir="${build}" />
	</target>

	<target name="compile" depends="init" description="compile the source">
		<!-- Compile the java code from ${src} into ${build} -->
		<echo message="classpaht=${unitexHome}/App/Unitex.jar"/>
		<javac includeAntRuntime="false" srcdir="${src}" destdir="${build}" classpath="${unitexHome}/App/Unitex.jar"/>
	</target>


	<target name="dist" depends="compile" description="generate the distribution">

		<mkdir dir="classes" />

		<copy todir="classes">
			<fileset dir="src/main/resources" />
		</copy>
		<copy todir="classes">
			<fileset dir="${build}" />
		</copy>

		<!-- Create the distribution directory -->
		<mkdir dir="${dist}" />

		<!-- create Gramlab.jar -->
		<jar jarfile="${dist}/Gramlab.jar" manifest="classes/fr/gramlab/Manifest.mf" basedir="classes" />

	</target>

	<target name="install" depends="dist">
		<copy file="${dist}/Gramlab.jar" todir="${appdir}" />
	</target>

	<target name="clean" description="clean up">
		<!-- Delete the ${build} and ${dist} directory trees -->
		<delete dir="${build}" />
		<!--delete dir="classes"/-->
		<delete includeemptydirs="true">
			<fileset dir="classes" includes="**/*" />
		</delete>
		<delete dir="${dist}" />
	</target>
</project>

