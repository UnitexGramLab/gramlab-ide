<project name="MyProject" default="dist" basedir=".">

  <description> simple example build file </description>

  <!-- set global properties for this build -->

  <property name="src" location="src"/>
  <property name="build" location="build"/>
  <property name="dist"  location="dist"/>
  <property name="lib" location="lib"/>
  <property name="appdir" location="${user.home}/Unitex2.1beta/App"/>
  
  <!-- targets -->
  <target name="init">
    <!-- Create the time stamp -->
    <tstamp/>
    <!-- Create the build directory structure used by compile -->
    <mkdir dir="${build}"/>
  </target>

  <target name="compile" depends="init" description="compile the source" >
    <!-- Compile the java code from ${src} into ${build} -->
    <javac srcdir="${src}" destdir="${build}" includes="${lib/*.jar}"/>
  </target>


  <target name="dist" depends="compile" description="generate the distribution" >

    <mkdir dir="classes"/> 

    <copy todir="classes">
      <fileset dir="resources"/>
    </copy>
    <copy todir="classes">
      <fileset dir="${build}"/>
    </copy>

    <!-- Create the distribution directory -->
    <mkdir dir="${dist}"/>

    <!-- create Unitex.jar -->
    <jar jarfile="${dist}/Unitex.jar" manifest="classes/fr/umlv/unitex/Manifest.mf"
      basedir="classes" excludes="classes/fr/loria/**"/>
  	
  	<!-- create XAlign.jar -->
  	<jar jarfile="${dist}/XAlign.jar" manifest="classes/fr/loria/Manifest.mf"
  	      basedir="classes" excludes="classes/fr/umlv/**"/>

  	<!-- These two targets will remain commented until their two manifest files
  	     are committed on the SVN server
    <jar jarfile="${dist}/Grf2ps.jar" manifest="classes/fr/umlv/unitex/grf2ps-manifest.mf"
      basedir="classes"/>

    <jar jarfile="${dist}/Grf2svg.jar" manifest="classes/fr/umlv/unitex/grf2svg-manifest.mf"
      basedir="classes"/>
      -->
  </target>

  <target name="install" depends="dist">
    <copy file="${dist}/Unitex.jar" todir="${appdir}"/>
    <copy file="${dist}/XAlign.jar" todir="${appdir}"/>
   <copy file="${lib}/xercesImpl.jar" todir="${appdir}"/>
   <copy file="${lib}/xmlParserAPIs.jar" todir="${appdir}"/>
  </target>

  <target name="clean" description="clean up" >
    <!-- Delete the ${build} and ${dist} directory trees -->
    <delete dir="${build}"/>
    <!--delete dir="classes"/-->
    <delete includeemptydirs="true">
      <fileset dir="classes" includes="**/*"/>
    </delete>
    <delete dir="${dist}"/>
  </target>
</project>

